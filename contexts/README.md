# project.yaml is the monorepo-level knowledge base for AI agents.
# Per-app context: contexts/{app-name}/ directory (e.g. ui/architecture.yaml, ui/features.yaml, ui/tech.yaml)
# See .agent/skills/context-sync/SKILL.md for sync specs.
